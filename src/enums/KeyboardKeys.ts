/**
 * Keyboard keys used for input mappings
 */
export enum KeyboardKey {
  // Arrow keys
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
  
  // WASD keys
  KeyW = 'KeyW',
  KeyA = 'KeyA',
  KeyS = 'KeyS',
  KeyD = 'KeyD',
  
  // Action keys
  Space = 'Space',
  ShiftLeft = 'ShiftLeft',
  ShiftRight = 'ShiftRight',
  Enter = 'Enter',
  Escape = 'Escape',
  Tab = 'Tab'
}

/**
 * Mouse button identifiers used in input mapping
 */
export enum MouseButton {
  Left = 'Mouse1',
  Right = 'Mouse2',
  Middle = 'Mouse3'
} 