# Debug settings
# Master debug toggle (primarily for logging, not a dependency for other features)
VITE_DEBUG_MODE=true

# Individual debug features (each can be enabled/disabled independently)
VITE_SHOW_PHYSICS_DEBUG=true  # Show physics collision wireframes and player state display
VITE_SHOW_FPS_COUNTER=true    # Show FPS counter in top-right corner  VITE_SHOW_DEBUG_PANEL=true    # Show debug control panels 
